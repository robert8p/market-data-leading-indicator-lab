name: Kalshi research runner CI

on:
  push:
    branches:
      - agent/kalshi-perps-research-runner-v1

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Decode verified application source
        shell: bash
        run: |
          set -euo pipefail
          rm -rf /tmp/kalshi-app
          mkdir -p /tmp/kalshi-app
          cat kalshi_bundle/part-*.b64 | base64 -d > /tmp/kalshi-source.tar.gz
          EXPECTED=$(cut -d' ' -f1 kalshi_bundle/SHA256SUMS)
          echo "$EXPECTED  /tmp/kalshi-source.tar.gz" | sha256sum -c -
          tar -xzf /tmp/kalshi-source.tar.gz -C /tmp/kalshi-app
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r requirements-kalshi.txt pytest pytest-asyncio
      - name: Compile and test packaged application
        shell: bash
        run: |
          set -euo pipefail
          PYTHONPATH=/tmp/kalshi-app python -m py_compile \
            scripts/kalshi_train_transfer_once.py \
            scripts/kalshi_research_service.py
          cd /tmp/kalshi-app
          python -m compileall -q kalshi_perps_app tests
          PYTHONPATH=. pytest -q
